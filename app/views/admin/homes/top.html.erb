<h4>注文履歴一覧</h4>
          <thead>
              <div class="form-group">
                <th>購入日時</th>
              </div>
              <div class="form-group">
                <th>購入者</th>
              </div>
              <div class="form-group">
                <th>注文個数</th>
              </div>
              <div class="form-group">
                <th>注文ステータス</th>
              </div>
            </tr>
          </thead>
              <tbody>
               <% @orders.each do |order| %>

              <tr>
                <td><%= order.created_at %></td>
                <td><%= order.shipping_name %></td>
                <td><%= order.id %></td>
                <td><%= order.status %></td>
              </tr>
              </tbody>
               <% end %>
