<h4>注文履歴の一覧</h4>
<table class="table table-bordered">
  <tbody>
    <tr>
      <td>注文日</td>
      <td>配送先</td>
      <td>注文商品</td>
      <td>支払金額</td>
      <td>ステータス</td>
      <td>注文詳細</td>
    </tr>
      <% @order.each do |order| %>
        <tr>
          <td><%= order.created_at %></td>
          <td><%= order.shipping_postal_code %><br>
              <%= order.shipping_address %><br>
              <%= order.shipping_name %></td>
          <td><%= order.cart_item.name %></td>
          <td><%= @total %></td>円
          <td><%= order.status %></td>
          <td><%= order.item.introduction %></td>
        </tr>
    <% end %>
  </tbody>
</table>